import AdminProductReviewDetailContent from "@/components/admin/AdminProductReviewDetailContent";

/**
 * Admin product review detail — view and manage a single review.
 * Layout (Navbar + AdminSidebar) from app/admin/layout.tsx.
 */
export default function AdminProductReviewDetailPage() {
  return <AdminProductReviewDetailContent />;
}
